'use strict';

const path = require('path');
const dirStructure = require('../dependencies/lib/dirStructure');

const paths = {};
let parent = undefined;

function loadPaths(parent, dirStructure){
  dirStructure.forEach((dir) => {
    
  });
}